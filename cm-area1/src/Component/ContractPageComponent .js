import "animate.css";
import { Contact } from "./ContactAtContactPage";

export const ContractPage = (className) => {
    return (
        <section className="banner-contactpage" id="home">
            <Contact/>
        </section>
    );
};
